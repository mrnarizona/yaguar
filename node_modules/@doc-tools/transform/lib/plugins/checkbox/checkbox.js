"use strict";
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.checkboxReplace = void 0;
const token_1 = __importDefault(require("markdown-it/lib/token"));
// eslint-disable-next-line no-useless-escape
const pattern = /^\[(X|\s|\_|\-)\]\s(.*)/i;
function matchOpenToken(tokens, i) {
    return (tokens[i].type === 'paragraph_open' &&
        tokens[i + 1].type === 'inline' &&
        tokens[i + 1].content.match(pattern));
}
const checkboxReplace = function (_md, opts) {
    let lastId = 0;
    const defaults = {
        divClass: 'checkbox',
        idPrefix: 'checkbox',
    };
    const options = Object.assign(defaults, opts);
    const createTokens = function (checked, label) {
        let token;
        const nodes = [];
        /**
         * <div class="checkbox">
         */
        token = new token_1.default('checkbox_open', 'div', 1);
        token.block = true;
        token.attrs = [['class', options.divClass]];
        nodes.push(token);
        /**
         * <input type="checkbox" id="checkbox{n}" checked="true">
         */
        const id = options.idPrefix + lastId;
        lastId += 1;
        token = new token_1.default('checkbox_input', 'input', 0);
        token.block = true;
        token.attrs = [
            ['type', 'checkbox'],
            ['id', id],
            ['disabled', ''],
        ];
        if (checked === true) {
            token.attrs.push(['checked', 'true']);
        }
        nodes.push(token);
        /**
         * <label for="checkbox{n}">
         */
        token = new token_1.default('checkbox_label_open', 'label', 1);
        token.attrs = [['for', id]];
        nodes.push(token);
        /**
         * content of label tag
         */
        token = new token_1.default('text', '', 0);
        token.content = label;
        nodes.push(token);
        /**
         * closing tags
         */
        token = new token_1.default('checkbox_label_close', 'label', -1);
        token.block = true;
        nodes.push(token);
        token = new token_1.default('checkbox_close', 'div', -1);
        token.block = true;
        nodes.push(token);
        return nodes;
    };
    const splitTextToken = function (matches) {
        let checked = false;
        const value = matches[1];
        const label = matches[2];
        if (value === 'X' || value === 'x') {
            checked = true;
        }
        return createTokens(checked, label);
    };
    return function (state) {
        const blockTokens = state.tokens;
        for (let i = 0; i < blockTokens.length; i++) {
            const match = matchOpenToken(blockTokens, i);
            if (!match) {
                continue;
            }
            blockTokens.splice(i, 3, ...splitTextToken(match));
        }
    };
};
exports.checkboxReplace = checkboxReplace;
//# sourceMappingURL=checkbox.js.map